# Cursor invocation

Active harness: **Cursor**.

## Invocation

- Skill / slash: `/handoff`
- Optional args: `standard`, `frontier`

Bare `/handoff` is the Cursor form (not `/handoff:handoff`). Shared `SKILL.md` owns the workflow.
